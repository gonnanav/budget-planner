import { createCategory } from "core/categories";

export const bills = createCategory({ id: "bills", name: "Bills" });
export const personal = createCategory({ id: "personal", name: "Personal" });
export const transportation = createCategory({
  id: "transportation",
  name: "Transportation",
});
