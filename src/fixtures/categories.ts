import { createCategory } from "@/core/categories";

export const transportation = createCategory(
  "category-transportation",
  "Transportation",
);

export const entertainment = createCategory(
  "category-entertainment",
  "Entertainment",
);

export const shopping = createCategory("category-shopping", "Shopping");
